1교시
 git clone 원격리포지토리주소
 git clone 원격리포지토리주소 .
 git config --list 

리눅스 명령어
vi편집기 화면이 나타나면 j(아래). k(위), Q(편집기 탈출)

2교시
다른 사람릐 원격리포지토리를 클론 후 새파일 추가 또는 기존파일 편집 후 로컬리포지토리에 커밋

원 저작자와 바인딩된 원격리포지토리 

확인 git remote -v
삭제 git remote remove origin
등록 git remote add origin 내가만든원격리포지토리주소
확인 git remot -v

업로드 git push
오류발생 (주소 새로 바인딩 필요함)
업로드 git push -u origin main

